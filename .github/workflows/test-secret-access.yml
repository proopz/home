name: Test Secret Access (Secure)

on: [push]  # Trigger on push events (optional, adjust as needed)

jobs:
  test_job:
    runs-on: ubuntu-latest  # Replace with appropriate environment (if needed)
    steps:
      - uses: actions/checkout@v3  # Fetches code from repository
      - run: echo "Trying to access secret (value won't be shown): ${{ secrets.TEST_SECRET }}"

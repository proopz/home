name: Test Secret Access (Secure)

on: [push]  # Trigger on push events

jobs:
  test_job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Trying to access secret (value won't be shown): ${{ secrets.AUTH_UN }}"
